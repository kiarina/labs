[project]
name = "pyannote-scd-speaker-segmentation"
version = "0.1.0"
description = "Detect speaker changes with Pyannote Segmentation 3.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "onnxruntime>=1.25.1",
]

[tool.uv]
package = false
