[project]
name = "silero-vad-segment-extraction"
version = "0.1.0"
description = "Detect and extract speech segments with Silero VAD"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "onnxruntime>=1.20",
]

[tool.uv]
package = false
