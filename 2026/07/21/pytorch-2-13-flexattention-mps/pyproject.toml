[project]
name = "pytorch-2-13-flexattention-mps"
version = "0.1.0"
description = "Benchmark PyTorch 2.13 FlexAttention against SDPA on Apple Silicon"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "numpy==2.3.5",
    "torch==2.13.0",
]

[tool.uv]
package = false
