[project]
name = "ecapa-tdnn-onnx-speaker-grouping"
version = "0.1.0"
description = "Group speaker segments using ECAPA-TDNN ONNX embeddings"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "onnxruntime>=1.25.1",
]

[tool.uv]
package = false
