name: gpt_live_relay
description: Minimal SDP relay for GPT-Live over WebRTC. Holds the API key; audio never passes through it.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.0

dev_dependencies:
  lints: ^6.0.0
