import 'package:flutter_web_plugins/flutter_web_plugins.dart';

/// The web backend is plain Dart (see src/backend_web.dart); there is no
/// method channel to register.
class MediaTapsWeb {
  static void registerWith(Registrar registrar) {}
}
