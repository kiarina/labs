#!/usr/bin/env bash
#MISE description="Probe TypeSafe Jev: API behavior, limits, and eight languages"
set -euo pipefail

if [[ -z "${TYPESAFE_AI_API_KEY:-}${TYPESAFE_API_KEY:-}" ]]; then
  echo "Set TYPESAFE_AI_API_KEY (or TYPESAFE_API_KEY) first." >&2
  exit 1
fi

uv run python probe_api.py
uv run python languages.py
