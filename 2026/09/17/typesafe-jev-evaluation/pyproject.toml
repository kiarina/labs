[project]
name = "typesafe-jev-evaluation"
version = "0.1.0"
description = "Probe TypeSafe AI's Jev System One model: responses, latency, limits, and languages"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "httpx==0.28.1",
]

[tool.uv]
package = false
