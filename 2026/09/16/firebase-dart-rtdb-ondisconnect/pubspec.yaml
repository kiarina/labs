name: firebase_dart_rtdb_ondisconnect
description: firebase_dart RTDB onDisconnect presence scenarios against the Firebase emulators.
version: 1.0.0

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  firebase_dart: 1.6.2
  http: ^1.6.0
  jose: ^0.3.5+2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
