#!/usr/bin/env bash
#MISE description="Analyze the plugin and the app, and build the web app"
set -euo pipefail

(cd media_taps && flutter pub get && flutter analyze)
cd app
flutter pub get
flutter analyze
flutter build web --release
