name: realtime_core
description: Pure Dart domain and application layer of the realtime media pipeline lab.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
